"""Pet Service Infrastructure Layer.

This module contains adapters that implement the ports defined in the
application layer. It handles all external concerns like HTTP APIs,
databases, and external services.

Components:
- adapters/in: Driving adapters (HTTP API, CLI, etc.)
- adapters/out: Driven adapters (repositories, external services)
"""
